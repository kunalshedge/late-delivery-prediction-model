# 1. Ignore EVERYTHING by default (the wildcard '*')
*

# 2. EXCEPTION 1: Do NOT ignore the plots folder and its contents
!plots/

# 3. EXCEPTION 2: Do NOT ignore the dataset file (Adjust the extension if needed)
!DataCoSupplyChainDataset.csv

# 4. EXCEPTION 3: Do NOT ignore the original Jupyter files
!late_delivery_prediction.ipynb
!late_delivery_prediction_model.ipynb

# 5. EXCEPTION 4: Do NOT ignore this file itself
!.gitignore